# Starter Java pipeline example
steps:
  - name: Install Java
    run: |
      sudo apt-get update
      sudo apt-get install -y openjdk-17-jdk
      java -version

  - name: Compile HelloWorld.java
    run: |
      javac HelloWorld.java

  - name: Run HelloWorld
    run: |
      java HelloWorld